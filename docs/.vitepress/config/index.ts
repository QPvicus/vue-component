import markdown from './markdown'
import nav from './nav'
import sidebar from './sidebar'
const config = {
	title: 'Vue-component',
	description: '组件库',
	markdown,
	themeConfig: {
		nav,
		sidebar
	}
}

export default config
